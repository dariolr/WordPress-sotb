# Credenziali WordPress per deploy in produzione
# Copia questo file come .env.secrets e inserisci i valori reali
# .env.secrets è già in .gitignore, non verrà mai versionato

WP_USER='sotb'
WP_PASS='LA_TUA_PASSWORD'
